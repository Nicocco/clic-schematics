{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsFlux",
  "title": "Flux Schema",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path where to create the component",
      "visible": false
    }
   },
  "required": [
    "name"
  ]
}