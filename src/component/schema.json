{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsComponent",
  "title": "Component Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the component",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the component?"
    },
    "splitGetSet": {
      "description": "True to split getter and seter regions",
      "default": false,
      "type":"boolean",
      "alias": "S"
    },
    "withoutStyleSheet": {
      "description": "Create a conponent whitout style sheet to use alternative style architecture in your app",
      "default": false,
      "type": "boolean",
      "alias": "s"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path where to create the component",
      "visible": false
    }
   },
  "required": [
    "name"
  ]
}